#!/usr/bin/env python3
"""
Jira Authentication Setup CLI
Mirrors JPAPI's setup command but for Jira
"""

import sys
from pathlib import Path

# Add the src directory to the path
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))

from scripts.setup.setup_jira_auth import main

if __name__ == "__main__":
    sys.exit(main())

